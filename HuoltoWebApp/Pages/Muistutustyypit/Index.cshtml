@page
@model HuoltoWebApp.Pages.Muistutustyypit.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Muistutustyyppi[0].MuistutustyyppiNimi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Muistutustyyppi) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MuistutustyyppiNimi)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MuistutustyyppiId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MuistutustyyppiId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MuistutustyyppiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
