@page
@model HuoltoWebApp.Pages.PvInfot.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PvInfo[0].InfoTxt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PvInfo[0].Kuva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PvInfo[0].Pv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PvInfo) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InfoTxt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kuva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pv.PvId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PvInfoId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PvInfoId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PvInfoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
